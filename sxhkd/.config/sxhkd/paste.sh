#!/usr/bin/env bash

# rewrite this without clipster

# ORIG=`clipster -c -o -n 1`
# NEW=`clipster -c -o -N $1`
#
# pkill clipster
#
# echo "$NEW" | xclip -sel c 
# xdotool key ctrl+v
# echo "$ORIG" | xclip -sel c 
#
# nohup clipster -d >/dev/null 2>&1  & 
